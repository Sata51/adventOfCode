pub trait ChallengeResolver {
    fn solve1(&self, input: String); // This should print the result of whatever the challenge is
    fn solve2(&self, input: String); // This should print the result of whatever the challenge is
}
